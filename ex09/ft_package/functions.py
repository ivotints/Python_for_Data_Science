def count_in_list(lst, item_to_count):
    """
    Count the number of occurrences of item_to_count in lst

    Args:
        lst: List to search in
        item_to_count: Item to count occurrences of

    Returns:
        Number of occurrences of item_to_count in lst
    """
    return lst.count(item_to_count)
